# Server-side only. Used by the Vercel serverless function at /api/analyze.
# Do NOT prefix with VITE_ (that would bundle it into the public frontend).
# Leave unset to run the app on its local rule engine with no external calls.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
